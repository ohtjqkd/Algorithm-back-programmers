-- 코드를 입력하세요
SELECT PD.PRODUCT_CODE, PD.PRICE * SUM(OS.SALES_AMOUNT) AS SALES FROM PRODUCT AS PD JOIN OFFLINE_SALE AS OS ON PD.PRODUCT_ID=OS.PRODUCT_ID GROUP BY PD.PRODUCT_ID ORDER BY SALES DESC, PD.PRODUCT_CODE ASC;